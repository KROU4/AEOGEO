[project]
name = "aeogeo-api"
version = "0.1.0"
description = "AEOGEO FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic>=2.0",
    "email-validator",
    "pydantic-settings",
    "python-dotenv",
    "sqlalchemy[asyncio]",
    "alembic",
    "asyncpg",
    "python-jose[cryptography]",
    "passlib[bcrypt]",
    "cryptography",
    "redis[hiredis]",
    "httpx",
    "pgvector",
    "pymupdf",
    "reportlab",
    "python-docx",
    "numpy",
    "python-multipart",
    "svix",
    "aeogeo-geo-audit",
]


[tool.uv.sources]
aeogeo-geo-audit = { path = "../geo-audit", editable = true }

[dependency-groups]
dev = [
    "ruff",
    "mypy",
    "pytest",
    "httpx",
    "aiosqlite",
    "types-reportlab",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true
